networks:
  default:
    name: xoxxox_netmid
    external: true
services:
  xoxxox_envgym:
    image: xoxxox/envgym:001
    container_name: xoxxox_envgym
    env_file:
      - ./envdir.txt
    volumes:
      - ../../../export:/opt/common
    command: bash -c "/usr/sbin/sshd -D"
